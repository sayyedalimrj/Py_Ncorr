# This file can be empty or used for package-level initializations.
# For example, creating and configuring the Flask app instance if not done in app.py directly.
# from .app import create_app
#
# app = create_app() # If you want a global app instance available on import