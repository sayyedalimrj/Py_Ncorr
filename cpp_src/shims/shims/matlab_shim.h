#ifndef NCORR_MATLAB_SHIM_H
#define NCORR_MATLAB_SHIM_H

/*  All legacy sources include **this** header, not mex.h directly.
    Keep it as a thin wrapper so there is only ONE definition of all
    the stub symbols.                                                     */
#include "mex.h"

#endif
